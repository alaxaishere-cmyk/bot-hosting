> ⛔ **EXPIRED** — the GitHub Actions job that served this preview has ended.
> Run `preview-tunnel.yml` again for a fresh URL.

# 🌐 Live public preview — temporary

**URL:** https://poet-attached-leaf-ranks.trycloudflare.com

**Login:** admin / demo-588970

Runner-side health check through the tunnel: HTTP 200

- Real limits, same code as the repo: 308 MiB RAM · 719 MiB disk · 25% CPU per account,
  one server, no sign-up — create accounts in the Admin tab.
- Disposable: it dies when this Actions job ends (this file then gets an EXPIRED banner)
  and nothing persists. Do not paste real bot tokens into a public demo.

_Fresh URL any time: `gh workflow run preview-tunnel.yml --ref <branch> -f hours=5`_
